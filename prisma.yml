#endpoint: ${env:PRISMA_ENDPOING}
endpoint: https://us1.prisma.sh/tim-trautman-e2ed19/buymymousse-backend/dev
datamodel: datamodel.prisma
#secret: ${env:PRISMA_SECRET}
hooks:
  post-deploy:
      - graphql get-schema -p prisma