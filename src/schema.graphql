type Dog {
    name: String!
}

""" This is a placeholder to get Yoga set up """
type Mutation {
    createDog(name: String!): Dog
}

""" This is a placeholder to get Yoga set up """
type Query {
    dogs: [Dog]!
}
